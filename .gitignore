# Build output
/anchor
/anchor-mcp
/anchor-lsp
/dist/
coverage.out
coverage.html

# Generated website assets (rebuild with `make web`)
web/site/anchor.wasm
web/site/wasm_exec.js

# Local catalogue cache (catalogue lives outside the repo)
.anchor.cache

# macOS
.DS_Store

# Editor / tooling
*.log
scratch/
node_modules/
package-lock.json
