# Build output
/askiso
/askiso-mcp
/askiso-lsp

# `go build ./examples/...` drops one binary per example into the working
# directory. They are named after their directory, so they are listed rather
# than matched by a pattern that could hide a real file.
/batch-audit
/cbpr-2026
/ci-sarif
/lookup
/mt-to-mx
/remediate
/validate
/dist/
coverage.out
coverage.html

# Generated website assets (rebuild with `make web`)
web/public/


# Local catalogue cache (catalogue lives outside the repo)
.askiso.cache

# macOS
.DS_Store

# Editor / tooling
*.log
scratch/
node_modules/
package-lock.json

# Generated: one page per message definition (make web)
web/content/messages/
